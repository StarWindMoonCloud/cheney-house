<!DOCUMENTTYPE html>
<html>
	<head>
		<title>Sep 25, 2013</title>
	</head>
	<body>
		<ol>
			<li>
				leetcode question and review
				<ul>
					<li>
						Merge Intervals
						<pre>
TestPlan
1. empty
2. one interval [1,2]
3. two intervals no overlap: [1,2]; [3,4]
4. two intervals with overlap: [1,3]; [2,4]; [3,5]
5. others

naive method 
two loops: O(n^2)

with sort
only need to check two consecutive elements
1. sort
2. check with the next element
a. overlap: merge
b. no overlap: move forward

Time O(n^2)

Can we perform better than sort method?

Points of attention
Comparison function parameter should be reference?
Be careful of itPre/itCur change when remove happens
Comparison function must be placed outside the class
</pre>
					</li>
					<li>
						Merge Intervals
						<pre>
Spiral Matrix II 

Spiral Matrix 

Testing
1. empty
2. one row
3. one column
4. normal
a. square
b. more rows
c. more columns

A: m*n
1,2,...,n
n+1, n+2,... 2n
.
.
.
(m-1)*n+1,...,mn

Recursively generate the outer layer
depth: 0, 1,...,
col, colOffset
row, rowOffset
1. top: m[0][0~col-1]
2. right: m[1~row-1][col-1]
3. bottom: m[row-1][col-2~0]
4. left: m[row-2~1][0]

Points of attention
How to pass matrix?
avoid same row(top, bottom)/column(right, left)
termination condition
</pre>
					</li>
				</ul>
			</li>
		</ol>
	</body>
</html>

