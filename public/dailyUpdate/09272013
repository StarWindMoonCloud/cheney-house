<!DOCUMENTTYPE html>
<html>
	<head>
		<title>Sep 27, 2013</title>
	</head>
	<body>
		<ol>
			<li>
				leetcode question and review
				<ul>
					<li>
						Unique Paths
						<pre>
Testing
1. (1,1)
2. one row
3. one column
4. normal

Mathematic method
horizontal: n-1 steps
vertical: m-1 steps
(m+n-2, m-1) = (m+n-2)!/((m-1)!*(n-1)!) = m*(m+1)*...(m+n-2)/(2*3*..*(n-1))
xChooseY(int x, int y)

DP
f(m,n) = 1
f(i,j) = f(i+1, j) + f(i, j+1)

It is a symmetric problem
We can assume start from (m,n) and go to (0,0)
f(0,0) = 1
f(i, j) = f(i-1, j) + f(i, j-1);
computer row by row (two rows are enough)
Time: O(m*n); Space: O(n); low cpu

Unique Paths II 
init:
first row: 1, 0 afterwards if 1 found in obstacleGrid
rowSteps[flag][col-1] & (!obstacleGrid[0][col])

first column: 1, 0 afterwards if 1 found in obstacleGrid

f(m, n) = (f(m-1,n) + (f(m, n-1)) & obstacleGrid(m,n)

Points of attention
one column case (include update of first column)
first row, first column (when obstacle)
obstacle on destination?
Do not apply bit operation on numbers greater than 1
</pre>
					</li>
					<li>
						Merge Intervals
						<pre>
Spiral Matrix II 

Spiral Matrix 

Testing
1. empty
2. one row
3. one column
4. normal
a. square
b. more rows
c. more columns

A: m*n
1,2,...,n
n+1, n+2,... 2n
.
.
.
(m-1)*n+1,...,mn

Recursively generate the outer layer
depth: 0, 1,...,
col, colOffset
row, rowOffset
1. top: m[0][0~col-1]
2. right: m[1~row-1][col-1]
3. bottom: m[row-1][col-2~0]
4. left: m[row-2~1][0]

Points of attention
How to pass matrix?
avoid same row(top, bottom)/column(right, left)
termination condition
</pre>
					</li>
				</ul>
			</li>
		</ol>
	</body>
</html>

